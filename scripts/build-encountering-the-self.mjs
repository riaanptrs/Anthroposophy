import fs from 'node:fs';
import path from 'node:path';
import {selfLessons,selfSource,selfConnections} from '../content/encountering-the-self.mjs';
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const n=id=>String(id).padStart(2,'0');
const rel=(from,to)=>path.relative(path.dirname(from),to).split(path.sep).join('/');
import {selfVisual} from '../content/encountering-the-self-visuals.mjs';
const archive=selfSource;
for(const lang of ['en','pt']){
 const pt=lang==='pt',base=pt?'docs/pt':'docs',other=pt?'docs':'docs/pt',home=`${base}/index.html`,dir=`${base}/encountering-the-self`;
 const title=pt?'O encontro com o eu':'Encountering the Self',label=pt?'Lição':'Lesson';
 const sourceNote=pt?'Fonte: Hermann Koepke, Encountering the Self: Transformation & Destiny in the Ninth Year, tradução de Jesse Darrell, 1989. Contribuição médica de Walter Holtzapfel. As páginas indicadas são da captura incompleta fornecida. Explicações, exemplos e atividades são originais do curso.':'Source: Hermann Koepke, Encountering the Self: Transformation & Destiny in the Ninth Year, translated by Jesse Darrell, 1989. Medical contribution by Walter Holtzapfel. Page assignments refer to the supplied incomplete capture. Explanations, examples and activities are original course material.';
 const favicon=fs.readFileSync(home,'utf8').match(/<link rel="icon"[^>]+>/)[0];
 const shell=(file,alt,heading,goal,main)=>`<!doctype html>
<html lang="${pt?'pt-BR':'en'}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(heading)} — ${title}</title><meta name="description" content="${esc(goal)}"><link rel="stylesheet" href="${rel(file,'docs/site-watercolour.css')}"><link rel="alternate" hreflang="${pt?'en':'pt-BR'}" href="${rel(file,alt)}"><link rel="stylesheet" href="${rel(file,'docs/temperaments.css')}">${favicon}</head><body><a class="skip" href="#main">${pt?'Pular para o conteúdo':'Skip to content'}</a><header><a class="brand" href="${rel(file,home)}#courses"><span class="mark" aria-hidden="true">✳</span> ${pt?'Antroposofia':'Anthroposophy'}</a><nav aria-label="${pt?'Idioma':'Language'}"><a lang="en" ${pt?'':'aria-current="page"'} href="${pt?rel(file,alt):path.basename(file)}">English</a><a lang="pt-BR" ${pt?'aria-current="page"':''} href="${pt?path.basename(file):rel(file,alt)}">Português</a></nav></header>${main}<footer><a href="${rel(file,home)}#courses">${pt?'Todos os cursos':'All courses'}</a><a href="${rel(file,alt)}" lang="${pt?'en':'pt-BR'}">${pt?'Read in English':'Ler em português'} →</a></footer></body></html>`;
 fs.mkdirSync(`${dir}/lessons`,{recursive:true});
 const file=`${dir}/index.html`,goal=pt?'Explore a consciência de si na infância e formas atentas de acompanhar suas mudanças.':'Explore childhood self-awareness and thoughtful ways of supporting change.';
 const main=`<main id="main"><p class="breadcrumb"><a href="../index.html#courses">← ${pt?'Todos os cursos':'All courses'}</a></p><section class="intro course-intro"><div class="eyebrow">Hermann Koepke · 1989 · ${pt?'Curso 8':'Course 8'}</div><h1>${title}</h1><p class="lead">${goal}</p><a class="button" href="lessons/00.html">${pt?'Começar pela orientação':'Begin with the orientation'} →</a><p class="closing">${pt?'Dezessete lições: conversas com pais, biografias, desenvolvimento, currículo, contribuição médica, quatro apêndices e síntese.':'Seventeen lessons: parent conversations, biographies, development, curriculum, the school doctor, four appendices and synthesis.'}</p></section><section class="path" id="lessons"><h2>${pt?'Seu percurso de estudo':'Your study path'}</h2>${selfLessons.map(l=>`<div class="lesson-row"><span class="number">${n(l.id)}</span><div><h3><a href="lessons/${n(l.id)}.html">${esc(l[lang].title)}</a></h3><p>${l.id===0?pt?'Orientação':'Orientation':l.id===16?pt?'Síntese original':'Original synthesis':l.id<5?pt?'Parte 1':'Part 1':l.id<12?pt?'Parte 2':'Part 2':pt?'Apêndice':'Appendix'}</p></div><a aria-label="${label} ${l.id}" href="lessons/${n(l.id)}.html">${pt?'Ler':'Read'} →</a></div>`).join('')}</section><aside class="source-note" id="sources"><h2>${pt?'Fontes e método':'Sources and method'}</h2><p>${sourceNote}</p><p>${pt?'A captura tem páginas em branco, passagens interrompidas e diagramas incompletos. As lições seguem a ordem do sumário e usam o texto disponível; as lacunas não foram reconstruídas. O link externo leva ao catálogo da editora, não ao texto integral.':'The capture has blank pages, interrupted passages and incomplete diagrams. Lessons follow the contents order using the available text; gaps have not been reconstructed. The external link leads to the publisher’s catalogue, not the full text.'}</p><p>${pt?'As conversas com pais são exemplos construídos, conforme as notas do livro. Distinguimos observação, interpretação antroposófica e adaptação educativa. Idade não equivale a ano escolar, e as afirmações médicas históricas não são instruções de tratamento.':'The parent conversations are constructed examples, as the book’s notes explain. We distinguish observation, anthroposophical interpretation and educational adaptation. Age is not the same as school grade, and historical medical claims are not treatment instructions.'}</p><p><a href="${archive}">${pt?'Localizar o livro no catálogo da editora':'Find the book in the publisher’s catalogue'} →</a></p></aside></main>`;
 fs.writeFileSync(file,shell(file,`${other}/encountering-the-self/index.html`,title,goal,main));
 for(const l of selfLessons){
  const v=l[lang],file=`${dir}/lessons/${n(l.id)}.html`,alt=`${other}/encountering-the-self/lessons/${n(l.id)}.html`,s={url:archive};
  for(const key of ['title','goal','key','context','example','explanation','activity','takeaway','terms','reading'])if(!v[key])throw Error(`Missing ${key}: Hermann Koepke ${l.id}/${lang}`);
  if(v.checks.length!==3)throw Error(`Expected three checks: Hermann Koepke ${l.id}/${lang}`);
  const comparison='';
  const content=`<main id="main" class="lesson-main"><p class="breadcrumb"><a href="../index.html#lessons">← ${pt?'Todas as lições de O encontro com o eu':'All Encountering the Self lessons'}</a></p><article><div class="eyebrow">Hermann Koepke · ${label} ${n(l.id)}</div><h1>${esc(v.title)}</h1><p class="lead">${esc(v.goal)}</p><div class="reading lesson-reading"><section class="worked-example"><h2>${pt?'Comece com uma situação':'Start with a situation'}</h2><p>${esc(v.example)}</p><p>${esc(v.explanation)}</p></section><h2>${pt?'A ideia principal da leitura':'The reading’s key idea'}</h2><p>${esc(v.key)}</p><p>${esc(v.context)}</p>${comparison}${selfVisual(l.id,lang)}<aside class="takeaway"><strong>${pt?'Guarde esta ideia':'Keep this idea'}</strong><p>${esc(v.takeaway)}</p></aside><aside class="source-note"><p>${pt?'Leitura':'Reading'}: ${esc(v.reading)}</p><p>${sourceNote}</p><p><a href="${s?s.url:archive}">${pt?'Localizar o livro no catálogo da editora':'Find the book in the publisher’s catalogue'} →</a> · <a href="../index.html#sources">${pt?'Sobre as edições':'About the editions'}</a></p></aside><h2>${pt?'Palavras da leitura':'Words for the reading'}</h2><p>${esc(v.terms)}</p><section class="practice"><h2>${pt?'Trabalhe com a ideia':'Work with the idea'}</h2><p>${esc(v.activity)}</p></section><h2>${pt?'Confira sua compreensão':'Check your understanding'}</h2>${v.checks.map(([q,a])=>`<div class="knowledge-check"><p>${esc(q)}</p><details><summary>${pt?'Ver uma resposta comentada':'Show a suggested answer'}</summary><p>${esc(a)}</p></details></div>`).join('')}<details><summary>${pt?'Como avaliar sua resposta':'How to assess your response'}</summary><p>${pt?'Confira quatro pontos: atribuição correta, distinção clara, razão apoiada na leitura e reconhecimento dos limites do exemplo. Releia o que ficou vago. Discordâncias fundamentadas podem demonstrar compreensão; não é preciso concordar nem relatar experiências espirituais.':'Check four things: correct attribution, a clear distinction, a reason supported by the reading and recognition of the example’s limits. Revisit anything vague. Reasoned disagreement can demonstrate understanding; agreement and reports of spiritual experiences are not required.'}</p></details><aside class="source-note"><h2>${pt?'Conecte com outro curso':'Connect with another course'}</h2><p><a href="${rel(file,`${base}/${l.bridge}`)}">${pt?'Revisar uma ideia relacionada':'Review a related idea'} →</a></p><p>${pt?'Compare os termos no contexto de cada livro. A ligação é um apoio ao estudo, não uma prova de que os argumentos sejam idênticos.':'Compare terms in each book’s context. This connection supports study; it does not prove that the arguments are identical.'}</p></aside></div></article><nav class="lesson-navigation" aria-label="${pt?'Navegação das lições':'Lesson navigation'}"><a href="${l.id?n(l.id-1)+'.html':'../index.html'}">← ${pt?'Anterior':'Previous'}</a><a href="../index.html#lessons">${pt?'Todas as lições':'All lessons'}</a><a href="${l.id<16?n(l.id+1)+'.html':'../index.html'}">${l.id<16?pt?'Próxima':'Next':pt?'Voltar ao curso':'Return to course'} →</a></nav></main>`;
  fs.writeFileSync(file,shell(file,alt,`${label} ${l.id}: ${v.title}`,v.goal,content));
 }
 let html=fs.readFileSync(home,'utf8').replace(/<!-- self-card:start -->[\s\S]*?<!-- self-card:end -->/g,'');
 const marker='</div></section><!-- courses:end -->';
 if(!html.includes(marker))throw Error(`Missing course insertion marker: ${home}`);
 const card=`<!-- self-card:start --><a class="course-card" href="encountering-the-self/index.html"><span class="eyebrow">${pt?'Curso 8 · Infância e consciência de si':'Course 8 · Childhood and self-awareness'}</span><h3>${title}</h3><p>${pt?'Dezessete lições sobre a transição dos nove anos: escuta, autonomia, currículo, imagens e avaliação das explicações.':'Seventeen lessons on the ninth-year transition: listening, independence, curriculum, images and evaluating explanations.'}</p></a><!-- self-card:end -->`;
 html=html.replace(marker,card+marker).replace(/<meta name="description" content="[^"]*">/,`<meta name="description" content="${pt?'Oito cursos bilíngues de antroposofia, com obras de Rudolf Steiner, Gilbert Childs e Hermann Koepke.':'Eight bilingual anthroposophy courses, featuring works by Rudolf Steiner, Gilbert Childs and Hermann Koepke.'}">`);
 fs.writeFileSync(home,html);
 for(const c of selfConnections){
  const target=`${base}/${c.target}`,v=c[lang];
  let html=fs.readFileSync(target,'utf8').replace(/<!-- self-connection:start -->[\s\S]*?<!-- self-connection:end -->/g,'');
  const block=`<!-- self-connection:start --><aside class="source-note self-connection"><h2>${esc(v[0])}</h2><p>${esc(v[1])}</p><p><a href="${rel(target,`${dir}/lessons/${n(c.lesson)}.html`)}">${pt?'Explorar a conexão em O encontro com o eu':'Explore the connection in Encountering the Self'} →</a></p></aside><!-- self-connection:end -->`;
  if(!html.includes('<aside class="takeaway">'))throw Error(`Missing connection insertion point: ${target}`);
  html=html.replace('<aside class="takeaway">',block+'<aside class="takeaway">');
  fs.writeFileSync(target,html);
 }
}
console.log('Built 34 Encountering the Self lesson pages, 2 indexes, and 6 bilingual lesson supplements.');
