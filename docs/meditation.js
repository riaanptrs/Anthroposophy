document.querySelectorAll('.verse-focus').forEach(button=>{const initial=button.textContent;button.addEventListener('click',()=>{const active=document.body.classList.toggle('verse-mode');button.closest('.meditation-text').classList.toggle('active',active);button.textContent=active?(document.documentElement.lang==='en'?'Return to the lesson':'Voltar à sessão'):initial;button.setAttribute('aria-pressed',String(active));if(active)button.closest('.meditation-text').scrollIntoView({block:'start'});});});
document.querySelectorAll('[data-download]').forEach(button=>button.addEventListener('click',()=>{const value=document.querySelector('#reflection').value;const pt=document.documentElement.lang!=='en';const status=document.querySelector('.download-status');if(!value.trim()){status.textContent=pt?'Escreva uma reflexão antes de baixar.':'Write a reflection before downloading.';return;}const body=document.title+'\n'+location.href+'\n\n'+value;const url=URL.createObjectURL(new Blob([body],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=button.dataset.download;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);status.textContent=pt?'Arquivo preparado para download.':'Reflection prepared for download.';}));
